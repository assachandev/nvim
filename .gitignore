# Lazy.nvim lock file (optional: commit this if you want reproducible plugin versions)
lazy-lock.json

# Optional plugins (local only)
lua/optional-plugins/
lua/plugins/molten.lua

# Claude Code local settings
guides/
